package practice.arrays;

public class ArrayExercise8 {
    /**
     *Rotate Matrix
     * Given an image represented by an NxN matrix write a method to rotate the image by 90 degrees.
     * public boolean rotateMatrix(int[][] matrix)
     * { if (matrix.length == 0 || matrix.length != matrix[0].length)
     * return false; int n = matrix.length;
     * for (int layer = 0; layer < n/2; layer++)
     * { int first = layer; int last = n - 1 - layer; c
     * for (int i=first; i
     */


    public static void main(String agrs[]) {
        int arr1[] = new int[]{1,2,3,4,5};
        int arr2[] = new int[]{5,1,2,3,4};

        //If sum & multiplication are equal then they r same


    }
}